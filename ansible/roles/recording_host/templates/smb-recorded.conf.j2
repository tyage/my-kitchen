[{{ samba_share_name }}]
   path = {{ samba_share_path }}
   browseable = yes
   read only = no
   guest ok = yes
   guest only = yes
   force user = {{ recording_user }}
   create mask = 0777
   directory mask = 0777
