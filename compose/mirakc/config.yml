epg:
  cache-dir: /var/lib/mirakc/epg

server:
  addrs:
    - http: '0.0.0.0:40772'

# Add the actual channels and tuner commands after installing the tuner driver.
# Keeping this file runnable but empty lets the API be verified before hardware
# is claimed by the new stack.
channels: []
tuners: []

# Example only:
# channels:
#   - name: NHK-G
#     type: GR
#     channel: '27'
# tuners:
#   - name: Tuner0
#     types: [GR]
#     command: recpt1 --device /dev/px4video0 {{{channel}}} - -
